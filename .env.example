AUTH_SECRET=cambia-este-secreto-de-sesion
STREAM_SECRET=cambia-este-secreto-de-stream
APP_URL=http://localhost:5000
PORT=5000

# SQL Server (Windows)
CONNECTION_STRING=Server=LARA-NB\SQLEXPRESS02;Database=CursoVentas;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True

# Mercado Pago Checkout Bricks (QR + tarjeta).
# Sin claves válidas, la app ofrece "Simular pago".
MP_PUBLIC_KEY=TEST-xxxxxxxx
MP_ACCESS_TOKEN=TEST-xxxxxxxx
# En producción: URL pública del webhook (si vacío usa APP_URL/api/webhooks/mercadopago)
MP_WEBHOOK_URL=
# false = solo pagos reales de Mercado Pago
MP_ALLOW_SIMULATE=false
APP_URL=http://localhost:5000
